{
  "name": "accord",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -ws --if-present",
    "lint": "npm run lint -ws --if-present",
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^24.1.3",
    "p-timeout": "^6.1.2",
    "semver": "^7.6.3",
    "typescript": "^5.6.2",
    "vitest": "^2.1.4",
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.2"
  }
}
